abstract final class CoreStrings {
  // ── Section scroll IDs ───────────────────────────────────────────────────────
  static const sectionHero = 'hero';
  static const sectionAbout = 'about';
  static const sectionExperience = 'experience';
  static const sectionWork = 'work';
  static const sectionProjects = 'projects';
  static const sectionSkills = 'skills';
  static const sectionContact = 'contact';

  // ── Routes ───────────────────────────────────────────────────────────────────
  static const routeHome = '/';
  static const routeExperience = '/experience';
  static const routeProjects = '/projects';

  // ── Router ───────────────────────────────────────────────────────────────────
  static const routeNameHome = 'home';

  // ── Skills sphere iframe ──────────────────────────────────────────────────────
  static const sphereViewType = 'skills-sphere';
  static const sphereMessageType = 'sphere_wheel';
  static const sphereEventTypeKey = 'type';
  static const sphereEventDeltaKey = 'deltaY';

  // ── Link schemes ──────────────────────────────────────────────────────────────
  static const mailtoScheme = 'mailto:';

  // ── Project link map keys ─────────────────────────────────────────────────────
  static const linkKeyGithub = 'github';
  static const linkKeyDownload = 'download';
  static const linkKeyDemo = 'demo';
  static const linkKeyPlaystore = 'playstore';
  static const linkKeyAppstore = 'appstore';
}
